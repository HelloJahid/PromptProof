"""PromptProof engine package.

The star of the project: a chained, gate-checked, feedback-looped prompting engine.
Phase 0 establishes the spine — model abstraction (llm), structured errors (errors),
and run tracing/observability (trace).
"""
