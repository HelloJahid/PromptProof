"""Minimal interface for the PromptProof engine.

Kept intentionally thin — the engine is the star. `cli.py` parses input, loads the API
keys from the environment, runs the chain, and prints the report.
"""
